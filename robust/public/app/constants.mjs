// Keep the browser defaults aligned with `lib/state-model.js` so local renders
// and server-backed normalization start from the same contract.
export const DEFAULT_STATE = {
  maxValue: 100000,
  currentValue: 1250
};

export const POLL_MS = 1500;
